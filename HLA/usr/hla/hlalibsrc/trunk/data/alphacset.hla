unit stdlib_data;
#include( "chars.hhf" )

namespace chars;

	readonly(16)
	
		AlphaChars:	cset; @nostorage;
			 cset { 'a'..'z', 'A'..'Z' };
			 cset {};
	 
end chars;

end stdlib_data;

													