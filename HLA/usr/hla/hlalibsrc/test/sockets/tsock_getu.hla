program sgetu_test; 
#include( "stdlib.hhf" )
#includeOnce( "sockets.hhf" )

static
	quit		:dword := 0;
	hostIPadrs	:dword;
	s			:server_t;
	i			:int32;
	timeout		:thunk;
	calls		:dword := 0;
	
	ipAdrsStr	:str.strvar(256);
	cmdLine		:str.strvar(256);
	exmsg		:str.strvar(256);
	
	
	
	procedure connected;
		@nodisplay;
		@nostackalign;
		@noframe;
	var
		handle		:dword;
		bufIndex	:dword;
		object		:pointer to server_t;
		ipAdrs		:hla.sockaddr;
		
		buffer		:char[64];
		
	begin connected;

		push( ebp );
		mov( esp, ebp );
		sub( _vars_, esp );
		
		pushad();
		pushfd();
		
		mov( eax, handle );
		mov( esi, object );
		
		/*****************************************************************/
		
		// Testing socket.getu32
		
		try
		
			object.getu32();
			if( eax <> 1 ) then
			
				raise($1011);
			
			endif;	
			
			object.getu32();
			if( eax <> 4294967295 ) then
			
				raise($1012);
			
			endif;	
					
			object.getu32();
			if( eax <> 2147483647 ) then
			
				raise($1014);
			
			endif;


			
			try
			
				object.getu32();

			  unprotected
			  
			  	// We we got to this point, we failed to raise a conversion error.
				
			  	raise($1005 );
				
			  exception( ex.ConversionError )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1006 );
				
			endtry;
			object.readLn();	// Clean up after ourselves.
			
			
			try
			
				object.getu32();

			  unprotected
			  
			  	// We we got to this point, we failed to raise an overflow error.
				
			  	raise($1015 );
				
			  exception( ex.ValueOutOfRange )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1016 );
				
			endtry;
			object.readLn();
			stderr.put( "socket.getu32 succeeded!" nl );
			
		  anyexception
		  
			stderr.put
			( 
				nl nl 
				"***************************************************" nl
				"socket.getu32 failed! eax=", eax, nl
				"Data read did not match data expected in file" nl
				"***************************************************" nl
				nl 
			);
			stdout.put( "Press Enter to Continue: " );
			stdin.readLn();
			os.exitProcess(1);
			
		endtry;








		// Testing f.getu16
		
		try
		
			object.getu16();
			if( eax <> 0 ) then
			
				raise($1021);
			
			endif;	
			
			object.getu16();
			if( eax <> 1 ) then
			
				raise($1022);
			
			endif;	
			
			object.getu16();
			if( eax <> 65535 ) then
			
				raise($1024);
			
			endif;	
			
			object.getu16();
			if( eax <> 32767 ) then
			
				raise($1025);
			
			endif;
			
			try
			
				object.getu16();

			  unprotected
			  
			  	// We we got to this point, we failed to raise a conversion error.
				
			  	raise($1005 );
				
			  exception( ex.ConversionError )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1006 );
				
			endtry;
			object.readLn();	// Clean up after ourselves.
			
			
			try
			
				object.getu16();

			  unprotected
			  
			  	// We we got to this point, we failed to raise an overflow error.
				
			  	raise($1026 );
				
			  exception( ex.ValueOutOfRange )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1027 );
				
			endtry;
			object.readLn();
			stderr.put( "socket.getu16 succeeded!" nl );
			
		  anyexception
		  
			stderr.put
			( 
				nl nl 
				"***************************************************" nl
				"socket.getu16 failed! eax=", eax, nl
				"Data read did not match data expected in file" nl
				"***************************************************" nl
				nl 
			);
			stdout.put( "Press Enter to Continue: " );
			stdin.readLn();
			os.exitProcess(1);
			
		endtry;





		// Testing socket.getu8
		
		try
		
			object.getu8();
			if( eax <> 0 ) then
			
				raise($1031);
			
			endif;	
			
			object.getu8();
			if( eax <> 1 ) then
			
				raise($1032);
			
			endif;	
			
			object.getu8();
			if( eax <> 255 ) then
			
				raise($1033);
			
			endif;	
			
			object.getu8();
			if( eax <> 127 ) then
			
				raise($1035);
			
			endif;
			
			try
			
				object.getu8();

			  unprotected
			  
			  	// We we got to this point, we failed to raise a conversion error.
				
			  	raise($1005 );
				
			  exception( ex.ConversionError )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1006 );
				
			endtry;
			object.readLn();	// Clean up after ourselves.
			
			
			try
			
				object.getu8();

			  unprotected
			  
			  	// We we got to this point, we failed to raise an overflow error.
				
			  	raise($1036 );
				
			  exception( ex.ValueOutOfRange )
			  
			  	// Okay, we succeeded if we got this exception.
				
			  anyexception
			  
			  	// Anything else? Pass it on.
				
			  	raise($1037 );
				
			endtry;
			object.readLn();
			stderr.put( "socket.getu8 succeeded!" nl );
			
		  anyexception
		  
			stderr.put
			( 
				nl nl 
				"***************************************************" nl
				"socket.getu8 failed! eax=", eax, nl
				"Data read did not match data expected in file" nl
				"***************************************************" nl
				nl 
			);
			stdout.put( "Press Enter to Continue: " );
			stdin.readLn();
			os.exitProcess(1);
			
		endtry;
			
		/*****************************************************************/
		
		object.close();
		mov( 1, quit );
		popfd();
		popad();
		leave();
		ret( _parms_ );
		
	end connected;
	
begin sgetu_test; 

	thunk timeout :=
		#{
			// On entry to thunk, EAX contains the address of the timeout
			// variable. Set this as desired for the timeout (2 seconds,
			// in this case).
			
			mov( 2, (type hla.timeval [eax]).tv_sec );
			stderr.put( "server", nl );
			
			// On the second call to this thunk, start the client application:
			
			cmp( calls, 1 );
			jne dontRunApp;
			
				#if( os.win32 )
				
					str.put
					(
						cmdLine,
						"cmd /C rclient socket_getu.data ",
						ipAdrsStr,
						" 8802"
					);
					stdout.put( "cmd=", cmdLine, "'" nl );
					
				#else
				
					str.put
					(
						cmdLine,
						"rclient.exe socket_getu.data ",
						ipAdrsStr,
						" 8802"
					);
					stdout.put( "cmd=", cmdLine, "'" nl );
					
				#endif
				os.bkgnd( cmdLine );
			
			dontRunApp:
			inc( calls );
			mov( quit, eax );
				
		}#;
	
	try
	
		mov( 0, i );
		s.create();
		
		sock.hostAdrs(); 
		mov( eax, hostIPadrs );
		sock.ipToStr( eax, ipAdrsStr );
		
		s.start
		( 
			hostIPadrs, 
			$8802, 
			timeout,
			&connected,
			0
		);
		
	  anyexception
	  
	  	ex.exceptionMsg( eax, exmsg );
	  	stdout.put( "Server Exception: ", exmsg, nl );
		fileio.close( s.handle );
		s.destroy();
		
	endtry;
	 
end sgetu_test; 
