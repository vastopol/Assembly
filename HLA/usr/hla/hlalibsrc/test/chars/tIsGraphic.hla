program test_isGraphic;
#include( "stdlib.hhf" )

static
	charset:char[256] :=
		[
			#for( i := 0 to 254 )
				char(i),
			#endfor
			#255
		];
		
	results:boolean[256] :=
		[
			#for( i := 0 to 254 )
			   char(i) >= ' ' & char(i) < #$7f,
			#endfor
			false
		];
		
begin test_isGraphic;

	
	for( mov( 0, ecx ); ecx<256; inc(ecx) )do
	
		chars.isGraphic( charset[ecx] );
		if( al <> results[ecx] ) then
		
			stderr.put
			( 
				nl nl 
				"***************************************************" nl
				"chars.isGraphic failed! al=", al, " ecx=", ecx, nl
				"***************************************************" nl
				nl 
			);
			stdout.put( "Press Enter to Continue: " );
			stdin.readLn();
			os.exitProcess(1);
			
		endif;
		
	endfor;
	stderr.put( "chars.isGraphic succeeded!" nl );
	os.exitProcess(0);
		
end test_isGraphic;
