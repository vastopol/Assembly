program test_isAlphanum;
#include( "stdlib.hhf" )

static
	charset:char[256] :=
		[
			#for( i := 0 to 254 )
				char(i),
			#endfor
			#255
		];
		
	results:boolean[256] :=
		[
			#for( i := 0 to 254 )
				@isalphanum(char(i)),
			#endfor
			false
		];
		
begin test_isAlphanum;

	
	for( mov( 0, ecx ); ecx<256; inc(ecx) )do
	
		chars.isAlphaNum( charset[ecx] );
		if( al <> results[ecx] ) then
		
			stderr.put
			( 
				nl nl 
				"***************************************************" nl
				"isalphanum failed! al=", al, " ecx=", ecx, nl
				"***************************************************" nl
				nl 
			);
			stdout.put( "Press Enter to Continue: " );
			stdin.readLn();
			os.exitProcess(1);
			
		endif;
		
	endfor;
	stderr.put( "chars.isAlphanum succeeded!" nl );
	os.exitProcess(0);
		
end test_isAlphanum;
