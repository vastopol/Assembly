program fputhex_test;
#include( "stdlib.hhf" )

var
	Handle		:dword;
	i			:int32;

static
	tbVal		:tbyte;
	tbVal2		:tbyte;
	
begin fputhex_test;

	mov( $1234_5678, eax );
	mov( eax, (type dword tbVal ));
	mov( eax, (type dword tbVal[4] ));
	mov( $90ab, (type word tbVal[8] ));
	
	mov( eax, (type dword tbVal2));
	mov( 0, (type dword tbVal2[4]));
	mov( 0, (type word tbVal2[8])); 

	fileio.openNew( "fputhex.txt" );
	mov( eax, Handle );

	conv.setUnderscores( false );

	fileio.puttb( Handle, tbVal );	
	fileio.newln( Handle );
	fileio.puttb( Handle, tbVal2 );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.puttbSize( Handle, tbVal, 30, '.' );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal, -30, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.puttbSize( Handle, tbVal2, 12, '.' );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal2, -12, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	conv.setUnderscores( true );
	
	fileio.puttb( Handle, tbVal );	
	fileio.newln( Handle );
	fileio.puttb( Handle, tbVal2 );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal, 30, '.' );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal, -30, '.' );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal2, 12, '.' );	
	fileio.newln( Handle );
	fileio.puttbSize( Handle, tbVal2, -12, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.putb( Handle, 0 );
	fileio.newln( Handle );
	fileio.putb( Handle, 1 );
	fileio.newln( Handle );
	fileio.putb( Handle, $7f );
	fileio.newln( Handle );
	fileio.putb( Handle, $80 );
	fileio.newln( Handle );
	fileio.putb( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putbSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putbSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putw( Handle, 0 );
	fileio.newln( Handle );
	fileio.putw( Handle, $10 );
	fileio.newln( Handle );
	fileio.putw( Handle, $200 );
	fileio.newln( Handle );
	fileio.putw( Handle, $7fff );
	fileio.newln( Handle );
	fileio.putw( Handle, $8000 );
	fileio.newln( Handle );
	fileio.putw( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putwSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putd( Handle, 0 );
	fileio.newln( Handle );
	fileio.putd( Handle, $10 );
	fileio.newln( Handle );
	fileio.putd( Handle, $200 );
	fileio.newln( Handle );
	fileio.putd( Handle, $3000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.putd( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );
	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putdSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putq( Handle, 0 );
	fileio.newln( Handle );
	fileio.putq( Handle, $10 );
	fileio.newln( Handle );
	fileio.putq( Handle, $200 );
	fileio.newln( Handle );
	fileio.putq( Handle, $3000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $8_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $90_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $A00_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $B000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $C_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $D0_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $E00_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $F000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putq( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putqSize( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putl( Handle, 0 );
	fileio.newln( Handle );
	fileio.putl( Handle, $10 );
	fileio.newln( Handle );
	fileio.putl( Handle, $200 );
	fileio.newln( Handle );
	fileio.putl( Handle, $3000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $8_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $90_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $A00_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $B000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $C_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $D0_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $E00_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $F000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $1_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $20_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $300_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $4000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $5_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $60_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $700_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $8000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $9_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $A0_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $B00_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $C000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $D_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $E0_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $F00_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $1000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putl( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -40, i); i<=40; inc(i)) do

		fileio.putlSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	conv.setUnderscores( false );


	fileio.putb( Handle, 0 );
	fileio.newln( Handle );
	fileio.putb( Handle, $7f );
	fileio.newln( Handle );
	fileio.putb( Handle, $80 );
	fileio.newln( Handle );
	fileio.putb( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putbSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putbSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.putw( Handle, 0 );
	fileio.newln( Handle );
	fileio.putw( Handle, $7fff );
	fileio.newln( Handle );
	fileio.putw( Handle, $8000 );
	fileio.newln( Handle );
	fileio.putw( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putwSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putwSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.putd( Handle, 0 );
	fileio.newln( Handle );
	fileio.putd( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.putd( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.putd( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.putdSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putdSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putq( Handle, 0 );
	fileio.newln( Handle );
	fileio.putq( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putq( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putq( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putqSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putqSize( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putl( Handle, 0 );
	fileio.newln( Handle );
	fileio.putl( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putl( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putl( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );

	for( mov( -40, i); i<=40; inc(i)) do

		fileio.putlSize( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $10_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $100_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.putlSize( Handle, $1000_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );
	fileio.newln( Handle );
	fileio.newln( Handle );
	
	
	
	
	
	
	

	conv.setUnderscores( false );

	fileio.puth80( Handle, tbVal );	
	fileio.newln( Handle );
	fileio.puth80( Handle, tbVal2 );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.puth80Size( Handle, tbVal, 30, '.' );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal, -30, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.puth80Size( Handle, tbVal2, 12, '.' );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal2, -12, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	conv.setUnderscores( true );
	
	fileio.puth80( Handle, tbVal );	
	fileio.newln( Handle );
	fileio.puth80( Handle, tbVal2 );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal, 30, '.' );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal, -30, '.' );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal2, 12, '.' );	
	fileio.newln( Handle );
	fileio.puth80Size( Handle, tbVal2, -12, '.' );	
	fileio.newln( Handle );
	fileio.newln( Handle );

	fileio.puth8( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth8( Handle, 1 );
	fileio.newln( Handle );
	fileio.puth8( Handle, $7f );
	fileio.newln( Handle );
	fileio.puth8( Handle, $80 );
	fileio.newln( Handle );
	fileio.puth8( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth8Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth16( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $10 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $200 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.puth16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth32( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $10 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $200 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $3000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.puth32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );
	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth64( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $10 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $200 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $3000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $8_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $90_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $A00_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $B000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $C_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $D0_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $E00_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $F000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puth64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puth64Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth128( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $10 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $200 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $3000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $4_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $50_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $600_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $7000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $8_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $90_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $A00_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $B000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $C_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $D0_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $E00_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $F000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $1_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $20_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $300_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $4000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $5_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $60_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $700_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $8000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $9_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $A0_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $B00_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $C000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $D_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $E0_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $F00_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $1000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puth128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -40, i); i<=40; inc(i)) do

		fileio.puth128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	conv.setUnderscores( false );


	fileio.puth8( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth8( Handle, $7f );
	fileio.newln( Handle );
	fileio.puth8( Handle, $80 );
	fileio.newln( Handle );
	fileio.puth8( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth8Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.puth16( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.puth16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.puth16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth16Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.puth32( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.puth32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.puth32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -10, i); i<=10; inc(i)) do

		fileio.puth32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth32Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth64( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puth64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puth64Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth64Size( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puth128( Handle, 0 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puth128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puth128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );

	for( mov( -40, i); i<=40; inc(i)) do

		fileio.puth128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $10_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $100_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.puth128Size( Handle, $1000_0000_0000_0000_0000_0000_0000_0000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );
	fileio.newln( Handle );
	fileio.newln( Handle );

	

	fileio.close( Handle );

end fputhex_test;

													