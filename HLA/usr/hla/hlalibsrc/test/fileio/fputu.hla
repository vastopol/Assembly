program fputu_test;
#include( "stdlib.hhf" )

var
	Handle		:dword;
	i			:int32;
	
begin fputu_test;

	
	fileio.openNew( "fputu.txt" );
	mov( eax, Handle );
	
	conv.setUnderscores( true );
	fileio.putu128Size( Handle, 10_000_000_000_000_000_000, 0, '.' );
	fileio.newln( Handle );
	
	

	conv.setUnderscores( true );

	fileio.putu8( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu8( Handle, 10 );
	fileio.newln( Handle );
	fileio.putu8( Handle, 200 );
	fileio.newln( Handle );
	fileio.putu8( Handle, $7f );
	fileio.newln( Handle );
	fileio.putu8( Handle, $80 );
	fileio.newln( Handle );
	fileio.putu8( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putu16( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.putu16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.putu16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );
	
	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 1000, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 10000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.putu32( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.putu32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.putu32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );
	
	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.putu64( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putu64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putu64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -28, i); i<=28; inc(i)) do

		fileio.putu64Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putu128( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putu128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putu128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );


	for( mov( -44, i); i<=44; inc(i)) do

		fileio.putu128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );



	conv.setUnderscores( false );

	fileio.putu8( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu8( Handle, $7f );
	fileio.newln( Handle );
	fileio.putu8( Handle, $80 );
	fileio.newln( Handle );
	fileio.putu8( Handle, $FF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu8Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putu16( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.putu16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.putu16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );
	
	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 1000, i, '.' );
		fileio.newln( Handle );
		fileio.putu16Size( Handle, 10000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.putu32( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.putu32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.putu32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	
	for( mov( -20, i); i<=20; inc(i)) do

		fileio.putu32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu32Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.putu64( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putu64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putu64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -24, i); i<=24; inc(i)) do

		fileio.putu64Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu64Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.putu128( Handle, 0 );
	fileio.newln( Handle );
	fileio.putu128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.putu128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.putu128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );

	for( mov( -44, i); i<=44; inc(i)) do

		fileio.putu128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.putu128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.close( Handle );

end fputu_test;

													