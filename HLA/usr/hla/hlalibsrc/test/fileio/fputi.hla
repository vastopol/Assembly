program fputi_test;
#include( "stdlib.hhf" )

var
	Handle		:dword;
	i			:int32;
	
begin fputi_test;


	fileio.openNew( "fputi.txt" );
	mov( eax, Handle );

	conv.setUnderscores( true );


	fileio.puti8( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti8( Handle, 127 );
	fileio.newln( Handle );
	fileio.puti8( Handle, -128 );
	fileio.newln( Handle );
	fileio.puti8( Handle, -1 );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti8Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.puti16( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.puti16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.puti16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti16Size( Handle, -10000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -1000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 1000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 10000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;

	fileio.puti32( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.puti32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.puti32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti32Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puti64( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puti64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puti64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );


	for( mov( -24, i); i<=24; inc(i)) do

		fileio.puti64Size( Handle, -1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puti128( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puti128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puti128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -44, i); i<=44; inc(i)) do

		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );



	conv.setUnderscores( false );

	fileio.puti8( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti8( Handle, 127 );
	fileio.newln( Handle );
	fileio.puti8( Handle, -128 );
	fileio.newln( Handle );
	fileio.puti8( Handle, -1 );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti8Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti8Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );


	fileio.puti16( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti16( Handle, $7fff );
	fileio.newln( Handle );
	fileio.puti16( Handle, $8000 );
	fileio.newln( Handle );
	fileio.puti16( Handle, $FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti16Size( Handle, -10000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -1000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 1000, i, '.' );
		fileio.newln( Handle );
		fileio.puti16Size( Handle, 10000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puti32( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti32( Handle, $7fff_ffff );
	fileio.newln( Handle );
	fileio.puti32( Handle, $8000_0000 );
	fileio.newln( Handle );
	fileio.puti32( Handle, $FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -20, i); i<=20; inc(i)) do

		fileio.puti32Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti32Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puti64( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti64( Handle, $7fff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puti64( Handle, $8000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puti64( Handle, $FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );
	fileio.newln( Handle );

	for( mov( -24, i); i<=24; inc(i)) do

		fileio.puti64Size( Handle, -1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti64Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.puti128( Handle, 0 );
	fileio.newln( Handle );
	fileio.puti128( Handle, $7fff_ffff_ffff_ffff_ffff_ffff_ffff_ffff );
	fileio.newln( Handle );
	fileio.puti128( Handle, $8000_0000_0000_0000_0000_0000_0000_0000 );
	fileio.newln( Handle );
	fileio.puti128( Handle, $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
	fileio.newln( Handle );

	for( mov( -44, i); i<=44; inc(i)) do

		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -100, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -10, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, -1, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 0, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 1_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 10_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.puti128Size( Handle, 100_000_000_000_000_000_000_000_000_000_000_000_000, i, '.' );
		fileio.newln( Handle );
		fileio.newln( Handle );

	endfor;
	fileio.newln( Handle );

	fileio.close( Handle );

end fputi_test;

													