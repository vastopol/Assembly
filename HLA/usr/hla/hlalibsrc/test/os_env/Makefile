all:
	hla -@@ -p:temp -obj:temp -x:tEnv.exe tEnv.hla
	tEnv.exe

thread:
	hla -@@ -thread -p:temp -obj:temp -x:tEnv.exe tEnv.hla
	tEnv.exe

clean:
	delete temp
	delete .inc
	delete .o
	delete .obj
	delete .link
	delete .exe
	delete .asm
	delete .pdb
	delete .ilk
	