program convsize_test;
#include( "stdlib.hhf" )	
	
static
	tb:tbyte;
		
begin convsize_test;

	conv.setUnderscores( true );
	
	conv.bSize( al );
	if( eax <> 2 ) then
	
		stderr.put( "conv.bSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.bSize w/underscores succeeded!" nl );

	conv.wSize( ax );
	if( eax <> 4 ) then
	
		stderr.put( "conv.wSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.wSize w/underscores succeeded!" nl );

	conv.dSize( eax );
	if( eax <> 9 ) then
	
		stderr.put( "conv.dSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.dSize w/underscores succeeded!" nl );

	conv.qSize( 0 );
	if( eax <> 19 ) then
	
		stderr.put( "conv.qSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.qSize w/underscores succeeded!" nl );

	conv.tbSize( tb );
	if( eax <> 24 ) then
	
		stderr.put( "conv.tbSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.tbSize w/underscores succeeded!" nl );

	conv.lSize( 0 );
	if( eax <> 39 ) then
	
		stderr.put( "conv.lSize w/underscores failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.lSize w/underscores succeeded!" nl );
	
			


	conv.setUnderscores( false );
	
	conv.bSize( al );
	if( eax <> 2 ) then
	
		stderr.put( "conv.bSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.bSize succeeded!" nl );

	conv.wSize( ax );
	if( eax <> 4 ) then
	
		stderr.put( "conv.wSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.wSize succeeded!" nl );

	conv.dSize( eax );
	if( eax <> 8 ) then
	
		stderr.put( "conv.dSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.dSize succeeded!" nl );

	conv.qSize( 0 );
	if( eax <> 16 ) then
	
		stderr.put( "conv.qSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.qSize succeeded!" nl );

	conv.tbSize( tb );
	if( eax <> 20 ) then
	
		stderr.put( "conv.tbSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.tbSize succeeded!" nl );

	conv.lSize( 0 );
	if( eax <> 32 ) then
	
		stderr.put( "conv.lSize failed!" nl );
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);

	endif;
	stderr.put( "conv.lSize succeeded!" nl );
	
			

	try

		mov( 0, al );
		conv.h8Size( al );
		if( eax <> 1 ) then

			raise( $1080 );

		endif;

		mov( $F, al );
		conv.h8Size( al );
		if( eax <> 1 ) then

			raise( $1081 );

		endif;

		mov( $10, al );
		conv.h8Size( al );
		if( eax <> 2 ) then

			raise( $1082 );

		endif;

		mov( $FF, al );
		conv.h8Size( al );
		if( eax <> 2 ) then

			raise( $1083 );

		endif;
		stdout.put( "conv.h8Size function succeeded" nl );

	  anyexception
	  
		stderr.put
		( 
			nl nl 
			"***************************************************" nl
			"conv.h8Size function failed! eax=", eax, nl
			"***************************************************" nl
			nl 
		);
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);
		
	endtry;	




	try

		mov( 0, ax );
		conv.h16Size( ax );
		if( eax <> 1 ) then

			raise( $1160 );

		endif;

		mov( $F, ax );
		conv.h16Size( ax );
		if( eax <> 1 ) then

			raise( $1161 );

		endif;

		mov( $10, ax );
		conv.h16Size( ax );
		if( eax <> 2 ) then

			raise( $1162 );

		endif;

		mov( $FF, ax );
		conv.h16Size( ax );
		if( eax <> 2 ) then

			raise( $1163 );

		endif;

		mov( $100, ax );
		conv.h16Size( ax );
		if( eax <> 3 ) then

			raise( $1164 );

		endif;

		mov( $FFF, ax );
		conv.h16Size( ax );
		if( eax <> 3 ) then

			raise( $1165 );

		endif;

		mov( $1000, ax );
		conv.h16Size( ax );
		if( eax <> 4 ) then

			raise( $1166 );

		endif;

		mov( $FFFF, ax );
		conv.h16Size( ax );
		if( eax <> 4 ) then

			raise( $1167 );

		endif;
		stdout.put( "conv.h16Size function succeeded" nl );

	  anyexception
	  
		stderr.put
		( 
			nl nl 
			"***************************************************" nl
			"conv.h16Size function failed! eax=", eax, nl
			"***************************************************" nl
			nl 
		);
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);
		
	endtry;	




	try

		mov( 0, eax );
		conv.h32Size( eax );
		if( eax <> 1 ) then

			raise( $1320 );

		endif;

		mov( $F, eax );
		conv.h32Size( eax );
		if( eax <> 1 ) then

			raise( $1321 );

		endif;

		mov( $10, eax );
		conv.h32Size( eax );
		if( eax <> 2 ) then

			raise( $1322 );

		endif;

		mov( $FF, eax );
		conv.h32Size( eax );
		if( eax <> 2 ) then

			raise( $1323 );

		endif;

		mov( $100, eax );
		conv.h32Size( eax );
		if( eax <> 3 ) then

			raise( $1324 );

		endif;

		mov( $FFF, eax );
		conv.h32Size( eax );
		if( eax <> 3 ) then

			raise( $1325 );

		endif;

		mov( $1000, eax );
		conv.h32Size( eax );
		if( eax <> 4 ) then

			raise( $1326 );

		endif;

		mov( $FFFF, eax );
		conv.h32Size( eax );
		if( eax <> 4 ) then

			raise( $1327 );

		endif;

		mov( $1_0000, eax );
		conv.h32Size( eax );
		if( eax <> 5 ) then

			raise( $1328 );

		endif;

		mov( $F_FFFF, eax );
		conv.h32Size( eax );
		if( eax <> 5 ) then

			raise( $1329 );

		endif;

		mov( $10_0000, eax );
		conv.h32Size( eax );
		if( eax <> 6 ) then

			raise( $2320 );

		endif;

		mov( $FF_FFFF, eax );
		conv.h32Size( eax );
		if( eax <> 6 ) then

			raise( $2321 );

		endif;

		mov( $100_0000, eax );
		conv.h32Size( eax );
		if( eax <> 7 ) then

			raise( $2322 );

		endif;

		mov( $FFF_FFFF, eax );
		conv.h32Size( eax );
		if( eax <> 7 ) then

			raise( $2323 );

		endif;

		mov( $1000_0000, eax );
		conv.h32Size( eax );
		if( eax <> 8 ) then

			raise( $2324 );

		endif;

		mov( $FFFF_FFFF, eax );
		conv.h32Size( eax );
		if( eax <> 8 ) then

			raise( $2325 );

		endif;
		stdout.put( "conv.h32Size function succeeded" nl );

	  anyexception
	  
		stderr.put
		( 
			nl nl 
			"***************************************************" nl
			"conv.h32Size function failed! eax=", eax, nl
			"***************************************************" nl
			nl 
		);
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);
		
	endtry;	






	try

		conv.h64Size( 0 );
		if( eax <> 1 ) then

			raise( $1640 );

		endif;

		conv.h64Size( $F );
		if( eax <> 1 ) then

			raise( $1641 );

		endif;

		conv.h64Size( $10 );
		if( eax <> 2 ) then

			raise( $1642 );

		endif;

		conv.h64Size( $FF );
		if( eax <> 2 ) then

			raise( $1643 );

		endif;

		conv.h64Size( $100 );
		if( eax <> 3 ) then

			raise( $1644 );

		endif;

		conv.h64Size( $FFF );
		if( eax <> 3 ) then

			raise( $1645 );

		endif;

		conv.h64Size( $1000 );
		if( eax <> 4 ) then

			raise( $1646 );

		endif;

		conv.h64Size( $FFFF );
		if( eax <> 4 ) then

			raise( $1647 );

		endif;

		conv.h64Size( $1_0000 );
		if( eax <> 5 ) then

			raise( $1648 );

		endif;

		conv.h64Size( $F_FFFF );
		if( eax <> 5 ) then

			raise( $1649 );

		endif;

		conv.h64Size( $10_0000 );
		if( eax <> 6 ) then

			raise( $2640 );

		endif;

		conv.h64Size( $FF_FFFF );
		if( eax <> 6 ) then

			raise( $2641 );

		endif;

		conv.h64Size( $100_0000 );
		if( eax <> 7 ) then

			raise( $2642 );

		endif;

		conv.h64Size( $FFF_FFFF );
		if( eax <> 7 ) then

			raise( $2643 );

		endif;

		conv.h64Size( $1000_0000 );
		if( eax <> 8 ) then

			raise( $2644 );

		endif;

		conv.h64Size( $FFFF_FFFF );
		if( eax <> 8 ) then

			raise( $2645 );

		endif;

		conv.h64Size( $1_0000_0000 );
		if( eax <> 9 ) then

			raise( $3640 );

		endif;

		conv.h64Size( $F_FFFF_FFFF );
		if( eax <> 9 ) then

			raise( $3641 );

		endif;

		conv.h64Size( $10_0000_0000 );
		if( eax <> 10 ) then

			raise( $3642 );

		endif;

		conv.h64Size( $FF_FFFF_FFFF );
		if( eax <> 10 ) then

			raise( $3643 );

		endif;

		conv.h64Size( $100_0000_0000 );
		if( eax <> 11 ) then

			raise( $3644 );

		endif;

		conv.h64Size( $FFF_FFFF_FFFF );
		if( eax <> 11 ) then

			raise( $3645 );

		endif;

		conv.h64Size( $1000_0000_0000 );
		if( eax <> 12 ) then

			raise( $3646 );

		endif;

		conv.h64Size( $FFFF_FFFF_FFFF );
		if( eax <> 12 ) then

			raise( $3647 );

		endif;

		conv.h64Size( $1_0000_0000_0000 );
		if( eax <> 13 ) then

			raise( $4640 );

		endif;

		conv.h64Size( $F_FFFF_FFFF_FFFF );
		if( eax <> 13 ) then

			raise( $4641 );

		endif;

		conv.h64Size( $10_0000_0000_0000 );
		if( eax <> 14 ) then

			raise( $4642 );

		endif;

		conv.h64Size( $FF_FFFF_FFFF_FFFF );
		if( eax <> 14 ) then

			raise( $4643 );

		endif;

		conv.h64Size( $100_0000_0000_0000 );
		if( eax <> 15 ) then

			raise( $4644 );

		endif;

		conv.h64Size( $FFF_FFFF_FFFF_FFFF );
		if( eax <> 15 ) then

			raise( $4645 );

		endif;

		conv.h64Size( $1000_0000_0000_0000 );
		if( eax <> 16 ) then

			raise( $4646 );

		endif;

		conv.h64Size( $FFFF_FFFF_FFFF_FFFF );
		if( eax <> 16 ) then

			raise( $4647 );

		endif;
		stdout.put( "conv.h64Size function succeeded" nl );

	  anyexception
	  
		stderr.put
		( 
			nl nl 
			"***************************************************" nl
			"conv.h64Size function failed! eax=", eax, nl
			"***************************************************" nl
			nl 
		);
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);
		
	endtry;	











	try

		conv.h128Size( 0 );
		if( eax <> 1 ) then

			raise( $1_1280 );

		endif;

		conv.h128Size( $F );
		if( eax <> 1 ) then

			raise( $1_1281 );

		endif;

		conv.h128Size( $10 );
		if( eax <> 2 ) then

			raise( $1_1282 );

		endif;

		conv.h128Size( $FF );
		if( eax <> 2 ) then

			raise( $1_1283 );

		endif;

		conv.h128Size( $100 );
		if( eax <> 3 ) then

			raise( $1_1284 );

		endif;

		conv.h128Size( $FFF );
		if( eax <> 3 ) then

			raise( $1_1285 );

		endif;

		conv.h128Size( $1000 );
		if( eax <> 4 ) then

			raise( $1_1286 );

		endif;

		conv.h128Size( $FFFF );
		if( eax <> 4 ) then

			raise( $1_1287 );

		endif;

		conv.h128Size( $1_0000 );
		if( eax <> 5 ) then

			raise( $1_1288 );

		endif;

		conv.h128Size( $F_FFFF );
		if( eax <> 5 ) then

			raise( $1_1289 );

		endif;

		conv.h128Size( $10_0000 );
		if( eax <> 6 ) then

			raise( $2_1280 );

		endif;

		conv.h128Size( $FF_FFFF );
		if( eax <> 6 ) then

			raise( $2_1281 );

		endif;

		conv.h128Size( $100_0000 );
		if( eax <> 7 ) then

			raise( $2_1282 );

		endif;

		conv.h128Size( $FFF_FFFF );
		if( eax <> 7 ) then

			raise( $2_1283 );

		endif;

		conv.h128Size( $1000_0000 );
		if( eax <> 8 ) then

			raise( $2_1284 );

		endif;

		conv.h128Size( $FFFF_FFFF );
		if( eax <> 8 ) then

			raise( $2_1285 );

		endif;

		conv.h128Size( $1_0000_0000 );
		if( eax <> 9 ) then

			raise( $3_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF );
		if( eax <> 9 ) then

			raise( $3_1281 );

		endif;

		conv.h128Size( $10_0000_0000 );
		if( eax <> 10 ) then

			raise( $3_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF );
		if( eax <> 10 ) then

			raise( $3_1283 );

		endif;

		conv.h128Size( $100_0000_0000 );
		if( eax <> 11 ) then

			raise( $3_1284 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF );
		if( eax <> 11 ) then

			raise( $3_1285 );

		endif;

		conv.h128Size( $1000_0000_0000 );
		if( eax <> 12 ) then

			raise( $3_1286 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF );
		if( eax <> 12 ) then

			raise( $3_1287 );

		endif;

		conv.h128Size( $1_0000_0000_0000 );
		if( eax <> 13 ) then

			raise( $4_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF_FFFF );
		if( eax <> 13 ) then

			raise( $4_1281 );

		endif;

		conv.h128Size( $10_0000_0000_0000 );
		if( eax <> 14 ) then

			raise( $4_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF_FFFF );
		if( eax <> 14 ) then

			raise( $4_1283 );

		endif;

		conv.h128Size( $100_0000_0000_0000 );
		if( eax <> 15 ) then

			raise( $4_1284 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF_FFFF );
		if( eax <> 15 ) then

			raise( $4_1285 );

		endif;

		conv.h128Size( $1000_0000_0000_0000 );
		if( eax <> 16 ) then

			raise( $4_1286 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF_FFFF );
		if( eax <> 16 ) then

			raise( $4_1287 );

		endif;

		conv.h128Size( $1_0000_0000_0000_0000 );
		if( eax <> 17 ) then

			raise( $5_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 17 ) then

			raise( $5_1281 );

		endif;

		conv.h128Size( $10_0000_0000_0000_0000 );
		if( eax <> 18 ) then

			raise( $5_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 18 ) then

			raise( $5_1283 );

		endif;

		conv.h128Size( $100_0000_0000_0000_0000 );
		if( eax <> 19 ) then

			raise( $5_1284 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 19 ) then

			raise( $5_1285 );

		endif;

		conv.h128Size( $1000_0000_0000_0000_0000 );
		if( eax <> 20 ) then

			raise( $5_1286 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 20 ) then

			raise( $5_1287 );

		endif;

		conv.h128Size( $1_0000_0000_0000_0000_0000 );
		if( eax <> 21 ) then

			raise( $6_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 21 ) then

			raise( $6_1281 );

		endif;

		conv.h128Size( $10_0000_0000_0000_0000_0000 );
		if( eax <> 22 ) then

			raise( $6_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 22 ) then

			raise( $6_1283 );

		endif;

		conv.h128Size( $100_0000_0000_0000_0000_0000 );
		if( eax <> 23 ) then

			raise( $6_1284 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 23 ) then

			raise( $6_1285 );

		endif;

		conv.h128Size( $1000_0000_0000_0000_0000_0000 );
		if( eax <> 24 ) then

			raise( $6_1286 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 24 ) then

			raise( $6_1287 );

		endif;

		conv.h128Size( $1_0000_0000_0000_0000_0000_0000 );
		if( eax <> 25 ) then

			raise( $7_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 25 ) then

			raise( $7_1280 );

		endif;

		conv.h128Size( $10_0000_0000_0000_0000_0000_0000 );
		if( eax <> 26 ) then

			raise( $7_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 26 ) then

			raise( $7_1283 );

		endif;

		conv.h128Size( $100_0000_0000_0000_0000_0000_0000 );
		if( eax <> 27 ) then

			raise( $7_1284 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 27 ) then

			raise( $7_1285 );

		endif;

		conv.h128Size( $1000_0000_0000_0000_0000_0000_0000 );
		if( eax <> 28 ) then

			raise( $7_1286 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 28 ) then

			raise( $7_1287 );

		endif;

		conv.h128Size( $1_0000_0000_0000_0000_0000_0000_0000 );
		if( eax <> 29 ) then

			raise( $8_1280 );

		endif;

		conv.h128Size( $F_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 29 ) then

			raise( $8_1281 );

		endif;

		conv.h128Size( $10_0000_0000_0000_0000_0000_0000_0000 );
		if( eax <> 30 ) then

			raise( $8_1282 );

		endif;

		conv.h128Size( $FF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 30 ) then

			raise( $8_1283 );

		endif;

		conv.h128Size( $100_0000_0000_0000_0000_0000_0000_0000 );
		if( eax <> 31 ) then

			raise( $8_1283 );

		endif;

		conv.h128Size( $FFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 31 ) then

			raise( $8_1284 );

		endif;

		conv.h128Size( $1000_0000_0000_0000_0000_0000_0000_0000 );
		if( eax <> 32 ) then

			raise( $8_1284 );

		endif;

		conv.h128Size( $FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF_FFFF );
		if( eax <> 32 ) then

			raise( $8_1285 );

		endif;


		stdout.put( "conv.h128Size function succeeded" nl );

	  anyexception
	  
		stderr.put
		( 
			nl nl 
			"***************************************************" nl
			"conv.h128Size function failed! eax=", eax, nl
			"***************************************************" nl
			nl 
		);
		stdout.put( "Press Enter to Continue: " );
		stdin.readLn();
		os.exitProcess(1);
		
	endtry;	





end convsize_test;
													